import { join } from 'path';

const directorio = '/ruta/principal';
const archivo = 'archivo.html';

const rutaCompleta = join(directorio, archivo);

console.log(rutaCompleta);

//\ruta\principal\archivo.html

